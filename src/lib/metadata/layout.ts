import { Metadata } from 'next';
import { SITE_NAME } from '../constants';

export const layoutMetadata: Metadata = {
  title: `${SITE_NAME} | home`,
  keywords: [
    ""
  ],
  description: "gargofx production site.",
};